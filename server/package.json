{
  "name": "professor-madrid-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Server-side proxy for Professor Madrid: holds LLM/TTS keys, verifies Supabase auth, enforces the treat wallet, meters usage.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "express": "^4.19.2",
    "ffmpeg-static": "^5.3.0",
    "google-auth-library": "^9.14.1"
  }
}
